package java010_abstract_interface.part06;

public interface Print {
	
	String toPaint();   // implements 한 class에서 사용해야 한다.
						// final이 붙은 변수는 상수이기 때문에 값을 지정해야함. 
						// public static final ( interface에서 default 값 )

}
